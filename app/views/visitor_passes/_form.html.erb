<%= simple_form_for @visitor_pass, url: user_visitor_passes_path, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :visitor_phone_number %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")), user_visitor_passes_path, :class => 'btn' %>
  </div>
<% end %>
